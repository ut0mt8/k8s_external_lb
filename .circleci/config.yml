version: 2
jobs:
  build:
    working_directory: /go/src/github.com/ut0mt8/k8s_external_lb
    docker:
      - image: circleci/golang:1.10.0

    steps:
      - checkout
      - run: go get -t -d -v ./...
      - run: go build -v
      - store_artifacts:
          path: /go/bin
          destination: raw-bin
